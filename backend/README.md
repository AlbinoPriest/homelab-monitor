# Backend

The Java 21 / Spring Boot backend will be created in Phase 1. No backend application exists at the Phase 0 checkpoint.

See `../docs/PROJECT_SPEC.md` and `../docs/ARCHITECTURE.md` before scaffolding it.
